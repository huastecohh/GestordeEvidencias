package com.example.gestordeevidencias.ui.theme

import androidx.compose.ui.graphics.Color

// === Ramp Índigo Profundo (Primario) ===
val Indigo900 = Color(0xFF1A1F36)
val Indigo800 = Color(0xFF252B4A)
val Indigo700 = Color(0xFF2E3860)
val Indigo600 = Color(0xFF3D52A0)   // PRIMARY — encabezados de tarjeta, botones CTA
val Indigo500 = Color(0xFF5569B8)
val Indigo400 = Color(0xFF7091E6)   // ACCENT — hovers, progreso, bordes activos
val Indigo100 = Color(0xFFCED6F5)
val Indigo50  = Color(0xFFEEF0FA)   // SURFACE INFO — chips activos, fondos de íconos

// === Ramp Teal Académico (Proceso / Éxito) ===
val Teal700   = Color(0xFF0D5C48)
val Teal600   = Color(0xFF1B8C6E)   // PROCESO — etiqueta "Proceso", check completado
val Teal50    = Color(0xFFE6F4F0)   // Fondo etiqueta Proceso

// === Ramp Ámbar Cálido (Revisión / Advertencia) ===
val Amber700  = Color(0xFFA85C00)
val Amber600  = Color(0xFFD97706)   // REVISIÓN — etiqueta "Revisión", advertencias
val Amber50   = Color(0xFFFEF3E2)   // Fondo etiqueta Revisión

// === Ramp Rosa / Error ===
val Rose600   = Color(0xFFD44C6E)
val Rose50    = Color(0xFFFCEEF2)

// === Neutrales Ink (Texto) ===
val Ink900    = Color(0xFF1A1F36)   // Texto primario (coincide con Indigo900)
val Ink700    = Color(0xFF4A5073)   // Texto secundario
val Ink400    = Color(0xFF8B92B3)   // Texto terciario / hints
val Ink200    = Color(0xFFCBCEDF)   // Bordes sutiles

// === Superficies ===
val Surface0  = Color(0xFFFFFFFF)   // Tarjetas, modales
val Surface1  = Color(0xFFF4F5FB)   // Fondo general de la app (blanco con 2% índigo)
val Surface2  = Color(0xFFECEEF8)   // Chips, separadores, fondos de input

// === Dark Mode ===
val DarkSurface0  = Color(0xFF12151F)
val DarkSurface1  = Color(0xFF1A1D2B)
val DarkSurface2  = Color(0xFF232638)
val DarkInk900    = Color(0xFFE8EAF6)
val DarkInk700    = Color(0xFFB0B5D4)
val DarkInk400    = Color(0xFF6B72A0)
val DarkBorder    = Color(0x1FFFFFFF)   // rgba(255,255,255,0.12)
